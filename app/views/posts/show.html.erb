<p>
  <h1><%= @posts.title %></h1>
</p>

<p><h3>by <%= @posts.user.name %></h3>
<i><%= @posts.updated_at.strftime("Updated last %a, %m/%d/%Y, %I:%M%p") %></i>
</p>

<p>
  <strong><%= @posts.body %></strong>
</p>

<h3><%= @posts.comments.count %> Comments</h3>

<%= render @posts.comments %>

<% if user_signed_in? %>
	<%= render 'layouts/comment_form' %>
<% else %>

<% end %>

